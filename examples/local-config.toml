# Example local vai configuration
# Place this file in your project directory as: vai.toml
# Local configs have the highest precedence and override preset and global configs

[tasks.dev]
description = "Start the application (local override)"
command = "pnpm run dev"

[tasks.compose]
description = "Start docker compose"
command = 'docker compose -p ${PROJECT:-$(pwd)}'
